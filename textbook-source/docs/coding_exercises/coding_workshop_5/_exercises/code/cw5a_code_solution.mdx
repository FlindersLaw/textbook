```
---
code: |
  temp = []
  # We iterate over all of the favourite things
  for item in favourite_things.keys():
    # First we check if this favourite thing is selected
    if favourite_things[item]:
      # If it is selected we add the appropriate template to temp
      if item == 'a_raindrops':
        temp.append(t_raindrops)

      # We have a followup question if we like whiskers on kittens
      elif item == 'b_kittens':
        if whiskers_followup == 'kittens':
          temp.append(t_kittens)
        elif whiskers_followup == 'chinchilla':
          temp.append(t_chinchilla)
        elif whiskers_followup == 'elephant':
          temp.append(t_elephant)
        elif whiskers_followup == 'catfish':
          temp.append(t_catfish)
        else:
          error_subject = 'Unknown followup question'
          error_content = 'Unknown for `whiskers_followup`: {}'.format(whiskers_followup)
          temp.append(t_error)

      # Now we get onto the rest of the elifs
      elif item == 'c_kettles':
        temp.append(t_kettles)
      elif item == 'd_mittens':
        temp.append(t_mittens)
      elif item == 'e_packages':
        temp.append(t_packages)
      else:
        error_subject = 'Unknown item'
        error_content = 'Do not have a template for item: `{}`'.format(item)
        temp.append(t_error)

  template_list = temp
---
```