export const title = 'Ex1a';
export const video = 'C1.01';

Make your own hello world program in your Playground:

- Add a new file
- Call it `ABC_hello_world.yml` (Replace *ABC* with your initials)
- Create a single mandatory question block that displays 'Hello world' (base your code on `hello_world.yml` in the video)